import { EncurtadorPipe } from './encurtador.pipe';

describe('EncurtadorPipe', () => {
  it('create an instance', () => {
    const pipe = new EncurtadorPipe();
    expect(pipe).toBeTruthy();
  });
});
